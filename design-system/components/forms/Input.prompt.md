Single-line text field with mono label and phosphor focus glow.

```jsx
<Input label="Work email" placeholder="you@lab.org" hint="We reply within a day." />
```

Props: `label`, `hint`, `mono` (default true). Forwards all native `<input>` attributes.
